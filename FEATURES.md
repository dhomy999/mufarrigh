<div dir="rtl">

# الوصف الوظيفي لميزات محرّر الفيديو العربي (aravid)

| | |
|---|---|
| **التطبيق** | Arabic Video Editor — محرّر فيديو بالتحرير النصّي |
| **التقنية** | Tauri 2 (Rust) + Next.js 16 + React 19 + Tailwind 4 |
| **المنصّات** | Windows / macOS / Linux — سطح مكتب فقط |
| **اللغة** | واجهة عربية كاملة باتجاه RTL |
| **آخر تحديث للوثيقة** | 2026-07-26 (مستخرَجة من الكود الفعلي) |

**الفكرة الأساسية:** يُفرَّغ صوت الفيديو إلى نصّ بطوابع زمنية لكل كلمة، فيصير حذف جزء من الفيديو مجرّد **حذف كلمات من النصّ** — تُترجَم آلياً إلى قصّ زمني في الملف النهائي.

---

## 1. سير العمل العام

```
اختيار الفيديو → استخراج الصوت → كشف السكتات → التفريغ الصوتي → استوديو التحرير → التصدير
                                                                      ↑
                                                          حفظ/استئناف تلقائي للمشروع
```

المراحل الأربع الأولى تظهر كبطاقات مرقّمة في الشاشة الرئيسية (`src/app/page.tsx`)، ثم يُفتح الاستوديو كشاشة مستقلّة.

---

## 2. استيراد الفيديو واستخراج الصوت

| الميزة | الوصف الوظيفي |
|---|---|
| **فحص FFmpeg** | يُفحص توفّر FFmpeg على النظام عند الإقلاع، ويظهر مؤشّر حالة في الشريط العلوي (جاهز / غير مثبّت). |
| **اختيار ملف فيديو** | نافذة اختيار من نظام التشغيل. الصيغ المدعومة: mp4, avi, mkv, mov, wmv, flv, webm, m4v, mpg, mpeg, ts, 3gp. |
| **عرض معلومات الملف** | اسم الملف وحجمه بالميجابايت بعد الاختيار، مع زرّ «تغيير». |
| **استخراج الصوت** | يستخرج المسار الصوتي كملف MP3 (128kbps / 44.1kHz) عبر FFmpeg، ويُحفظ في مجلّد الكاش الخاص بالتطبيق. |
| **رسائل خطأ مفهومة** | تُترجَم أخطاء FFmpeg إلى عربية واضحة: «الفيديو لا يحتوي على مسار صوتي»، «الملف تالف أو بصيغة غير مدعومة»… |

---

## 3. كشف السكتات والصمت

| الميزة | الوصف الوظيفي |
|---|---|
| **كشف تلقائي** | يستخدم فلتر `silencedetect` في FFmpeg لتحديد كل فترات الصمت في المسار الصوتي. |
| **معاملات قابلة للضبط** | حدّ الضوضاء بالديسيبل (افتراضي −30dB) والحدّ الأدنى لمدّة الصمت بالثواني (افتراضي 0.5s). |
| **ملخّص إحصائي** | عدد السكتات، إجمالي مدّة الصمت، ونسبة الصمت من الملف. |
| **قائمة تفصيلية** | كل سكتة بترتيبها ووقت بدايتها ونهايتها ومدّتها. |
| **معاينة صوتية** | زرّ تشغيل/إيقاف لكل سكتة يُسمعك النطاق المكتشَف بالضبط (بلا هوامش) للحكم على دقّة الكشف. |
| **تحديد ما يُحذف** | كل السكتات محدَّدة للحذف افتراضياً؛ يمكن التراجع عن أيّ منها فردياً، أو «تحديد الكل» / «إلغاء الكل». |
| **الترحيل للاستوديو** | السكتات المحدَّدة تنتقل كفترات مستبعَدة تُدمَج مع الكلمات المحذوفة عند التصدير. |

---

## 4. التفريغ الصوتي (Speech-to-Text)

| الميزة | الوصف الوظيفي |
|---|---|
| **ثلاثة مزوّدين** | **Groq** (whisper-large-v3 / turbo)، **OpenAI** (whisper-1، gpt-4o-transcribe، gpt-4o-mini-transcribe)، **Speechmatics** (enhanced / standard). |
| **طوابع زمنية للكلمات** | يُرجَع النصّ ككلمات منفصلة، لكلٍّ منها وقت بداية ونهاية — أساس التحرير النصّي كلّه. |
| **درجات الثقة** | Speechmatics يُرجع درجة ثقة لكل كلمة (تُستخدم في التلوين داخل المحرّر)؛ Whisper لا يُرجعها. |
| **واجهة Speechmatics غير المتزامنة** | تُدار كمهمة (رفع → استعلام دوري → جلب النتيجة json-v2) وقد تستغرق دقائق. |
| **اللغة الافتراضية** | `ar` (العربية)، مع دعم تقني لتمرير لغة أخرى. |
| **مؤشّر جاهزية المفتاح** | يعرض ما إذا كان مفتاح المزوّد المختار مضبوطاً واسم الموديل المستخدَم، مع رابط سريع للإعدادات. |
| **نتائج فورية** | عدد الكلمات، المدّة، اللغة، النصّ الكامل، وقائمة الكلمات بطوابعها الزمنية. |

---

## 5. استوديو التحرير النصّي

قلب التطبيق — شاشة مقسّمة: مشغّل الفيديو + محرّر النصّ + لوحات جانبية.

### 5.1 محرّر النصّ التفاعلي

| الميزة | الوصف الوظيفي |
|---|---|
| **عرض الكلمات** | كامل التفريغ ككلمات قابلة للنقر باتجاه RTL. |
| **التحديد** | نقر مفرد = كلمة واحدة · Shift+نقر = نطاق · Ctrl/Cmd+نقر = تحديد متعدّد · Ctrl+A = الكل · Esc = إلغاء التحديد. |
| **الحذف الناعم** | Delete/Backspace يشطب الكلمات المحدَّدة (خطّ شطب رمادي) دون فقدان بياناتها. |
| **الاستعادة** | نقر مزدوج على كلمة محذوفة يعيدها. |
| **التحرير الإنلاين** | نقر مزدوج على كلمة نشطة يفتح حقل تعديل نصّها؛ Enter يحفظ، Esc يلغي. الكلمة المعدّلة تحتفظ بتوقيتها الأصلي وتُوسَم بخطّ منقّط. |
| **القفز الزمني** | النقر على أي كلمة ينقل الفيديو إلى وقتها (وإن كانت داخل فترة محذوفة يُقفَز لنهايتها). |
| **إبراز الكلمة الجارية** | الكلمة المُشغَّلة حالياً تُبرَز بخلفية مميّزة أثناء التشغيل. |
| **تلوين الثقة المنخفضة** | مع Speechmatics تُظلَّل الكلمات التي شكّ فيها النموذج: أحمر (<50%) وكهرماني (<80%)، مع عدّاد ومفتاح تشغيل/إيقاف ودليل ألوان. تُلغى العلامة عن الكلمة بعد تعديلها يدوياً. |
| **إحصائيات حيّة** | عدد الكلمات النشطة والمحذوفة والمحدَّدة في شريط المحرّر، والمدّة المحذوفة والمدّة النهائية في الشريط العلوي. |

### 5.2 المشغّل الذكي

| الميزة | الوصف الوظيفي |
|---|---|
| **تخطّي المحذوف تلقائياً** | حلقة مراقبة بمعدّل 60 مرة/ثانية (`requestAnimationFrame`) تقفز فوق أي فترة محذوفة أثناء التشغيل — فترى معاينة حيّة للنتيجة النهائية قبل التصدير. |
| **قطع سلس (Crossfade)** | خفت/ظهور للصوت بمدّة 25ms حول كل قفزة عبر Web Audio API، لمنع «فرقعة» الصوت. |
| **مؤشّر القفزة** | وميض بصري يعرض «قفز من … إلى …» عند كل تخطٍّ. |
| **شريط زمني بصري** | يعرض الفترات المحذوفة كمناطق حمراء على الخطّ الزمني، مع نقر للانتقال (يحترم اتجاه RTL). |
| **تحكّم كامل** | تشغيل/إيقاف، ±10 ثوانٍ، كتم، شريط مستوى الصوت، وعرض الوقت الحالي/الكلي. Space = تشغيل/إيقاف في كل أنحاء الاستوديو. |

### 5.3 مراجعة التفريغ بالذكاء الاصطناعي

| الميزة | الوصف الوظيفي |
|---|---|
| **كشف الأخطاء** | يُرسَل التفريغ لنموذج نصّي (OpenAI / Anthropic / Gemini حسب اختيارك) لاكتشاف الكلمات غير المنطقية والتحريف الصوتي والأسماء المشوّهة. |
| **تعليم بصري** | الكلمات المشبوهة تُوسَم بخطّ متعرّج ملوّن حسب الخطورة: أحمر (عالية)، كهرماني (متوسطة)، أزرق (منخفضة) — كمدقّق Word. |
| **لوحة جانبية** | لكل موضع: النصّ المشبوه، التصحيح المقترح، سبب الاشتباه، ونطاقه الزمني (قابل للنقر للانتقال إليه). |
| **قبول التصحيح** | يطبّق الاقتراح على كلمات النطاق مع الحفاظ على التوقيت الأصلي (لا يُقتطع صوت). |
| **تجاهل** | إزالة العلامة دون تغيير النصّ. |
| **إزالة تلقائية** | تصحيح الكلمة يدوياً يُزيل علامة الخطأ عنها تلقائياً. |

---

## 6. التصدير والمخرجات

### 6.1 تصدير الفيديو النهائي

| الميزة | الوصف الوظيفي |
|---|---|
| **قصّ دقيق** | تُحذف كل الفترات المستبعَدة (كلمات محذوفة + سكتات محدَّدة) عبر فلاتر `trim`/`atrim` + `concat` في FFmpeg، مع حفاظ كامل على تزامن الصوت والصورة. |
| **معالجة الحدود** | دمج الفترات المتداخلة، قصّها على حدود الفيديو، وتجاهل الأجزاء الأقصر من 10ms. |
| **جودة الإخراج** | H.264 (preset fast، CRF 20) + AAC 192k مع `+faststart`. يعمل أيضاً على فيديو بلا مسار صوتي. |
| **ملخّص النتيجة** | حجم الملف، المدّة الأصلية، المدّة النهائية، ورسالة بمقدار الثواني المحذوفة. |
| **فتح مجلّد المخرجات** | زرّ يفتح مستكشف الملفات مع تحديد الملف (يدعم Windows/macOS/Linux). |
| **حماية من الحالات الفارغة** | رسائل واضحة عند عدم وجود أجزاء محذوفة أو عند تحديد كامل الفيديو للحذف. |

### 6.2 تصدير الترجمة (SRT / WebVTT)

| الميزة | الوصف الوظيفي |
|---|---|
| **بناء الأسطر تلقائياً** | تُجمَّع الكلمات النشطة في أسطر ترجمة وفق: فجوة صمت > 0.7s، أو مدّة > 5s، أو طول > 42 حرفاً. |
| **يحترم تحريرك** | يستخدم الكلمات بحالتها الحالية (المعدّلة والمحذوفة) لا التفريغ الخام. |
| **مطابقة التوقيت** | خيار إزاحة التوقيتات لتطابق الفيديو **بعد** الحذف (مفعّل افتراضياً)، أو إبقائها على توقيت الفيديو الأصلي. |
| **صيغتان** | تنزيل `.srt` أو `.vtt` باسم مشتقّ من اسم الفيديو، مع عرض عدد الأسطر والكلمات. |

### 6.3 توليد المقاطع القصيرة (Shorts)

| الميزة | الوصف الوظيفي |
|---|---|
| **اقتراح ذكي** | يحلّل نموذج نصّي التفريغ ويقترح 3–5 مقاطع (10–60 ثانية) من أفضل اللحظات. |
| **معلومات كل مقطع** | عنوان جذّاب بالعربية، النطاق الزمني، المدّة، وسبب الاختيار. |
| **استخراج فوري** | زرّ يستخرج أي مقطع كملف MP4 منفصل (H.264 CRF 21 + AAC 192k) في مجلّد `shorts`، مع زرّ فتح المجلّد. |

### 6.4 تصدير بيانات التحرير (JSON)

زرّ سريع في شريط الاستوديو ينزّل ملف JSON يحوي: مسار الفيديو، الفترات المستبعَدة، الكلمات المحذوفة بطوابعها، وإحصائيات المدد — للاستخدام في أدوات خارجية.

---

## 7. حفظ المشاريع واستئنافها

| الميزة | الوصف الوظيفي |
|---|---|
| **حفظ تلقائي** | يُحفظ المشروع تلقائياً (بتأخير ~1.2 ثانية) بعد أي تغيير مهمّ، ولا يقاطع عملك عند الفشل. يبدأ الحفظ بعد اكتمال التفريغ فقط. |
| **معرّف ثابت** | معرّف المشروع مشتقّ من مسار الفيديو (djb2)، فإعادة العمل على نفس الفيديو تُحدِّث المشروع بدل تكراره. |
| **ما يُحفَظ** | معلومات الفيديو، مسار الصوت المستخرَج، معاملات كشف السكتات ونتائجها والمحدَّد منها، التفريغ الكامل، وكلمات المحرّر بحالتها (حذف + تعديلات). |
| **قائمة «مشاريعك الأخيرة»** | تظهر في الشاشة الرئيسية مرتّبة من الأحدث: الاسم، تاريخ آخر تحديث، عدد الكلمات والمحذوف منها، المدّة، وتنبيه **⚠ ملف الفيديو مفقود** إن نُقل الملف الأصلي. |
| **فتح / حذف** | «فتح» يستعيد كامل الحالة ويقفز للمرحلة الصحيحة؛ «حذف» يزيل المشروع فقط ولا يمسّ ملف الفيديو. |
| **مكان التخزين** | ملف JSON واحد لكل مشروع في مجلّد بيانات التطبيق، مع تنظيف المعرّف حمايةً من اجتياز المسارات. |

---

## 8. الإعدادات

| القسم | الوصف الوظيفي |
|---|---|
| **مفاتيح التفريغ الصوتي** | مفتاح API + الموديل المختار لكل مزوّد (Groq / OpenAI / Speechmatics)، مع مؤشّر «مضبوط» واقتراحات موديلات قابلة للكتابة يدوياً. |
| **سجلّ النماذج النصية** | قائمة مرنة من النماذج، لكلٍّ منها: اسم مخصّص + مزوّد (OpenAI / Anthropic / Gemini) + معرّف الموديل + مفتاحه الخاص. يمكن الإضافة والحذف. |
| **مهام النماذج النصية** | مهمّتان — «كشف الأخطاء» و«توليد Shorts» — تُربَط كلٌّ منهما بنموذج من السجلّ. |
| **برومت قابل للتعديل** | لكل مهمّة برومت نظام كامل قابل للتحرير مع زرّ استعادة الافتراضي. |
| **حفظ ومزامنة فورية** | يُحفظ في `localStorage` وتتزامن التغييرات فوراً بين كل المكوّنات والنوافذ المفتوحة. |
| **ترحيل تلقائي** | تُهاجَر الإعدادات القديمة إلى البنية الجديدة عند التحديث دون فقدان المفاتيح. |

---

## 9. سلوكيات عامة

| المجال | الوصف |
|---|---|
| **الخصوصية** | الفيديو والصوت لا يُرفعان لأي خادم إطلاقاً عدا ملف الصوت المُرسَل لمزوّد التفريغ الذي تختاره؛ نماذج الـ LLM تستقبل **النصّ فقط**. كل معالجة الفيديو محلّية عبر FFmpeg. |
| **معالجة الأخطاء** | نوع خطأ موحّد (`AppError`) من الخلفية مع رسالة عربية وتفاصيل تقنية اختيارية تُعرض عند الحاجة. |
| **يعمل على سطح المكتب فقط** | يُكتشَف غياب بيئة Tauri وتُعرض رسالة واضحة بدل الفشل الصامت. |
| **الأداء** | بحث ثنائي في الفترات المستبعَدة، كتابة فلتر FFmpeg في ملف لتفادي حدود طول الأوامر في ويندوز، وفحص سريع للوسائط بلا فكّ ترميز. |

### اختصارات لوحة المفاتيح

| الاختصار | الوظيفة |
|---|---|
| `Space` | تشغيل / إيقاف الفيديو |
| نقر | تحديد كلمة + القفز لوقتها |
| `Shift` + نقر | تحديد نطاق كلمات |
| `Ctrl/Cmd` + نقر | إضافة/إزالة كلمة من التحديد |
| `Ctrl` + `A` | تحديد كل الكلمات النشطة |
| `Delete` / `Backspace` | حذف ناعم للمحدَّد |
| نقر مزدوج | تعديل الكلمة (أو استعادتها إن كانت محذوفة) |
| `Enter` / `Esc` | تأكيد / إلغاء التعديل الإنلاين |

---

## 10. قيود الإصدار الحالي

- **FFmpeg نظامي مطلوب** — لا يُضمَّن مع التطبيق بعد.
- **مجلّد المخرجات غير قابل للاختيار** — الفيديو المُصدَّر والمقاطع القصيرة تُكتب في مجلّد كاش التطبيق (يمكن الوصول إليها بزرّ «فتح مجلّد المخرجات»).
- **لا شريط تقدّم ولا إلغاء** للتصدير أو التفريغ — عمليات طويلة بلا مؤشّر نسبة.
- **حدّ حجم التفريغ** — واجهات Whisper ترفض الملفات فوق 25MB (≈ 25 دقيقة بجودة الاستخراج الحالية)؛ لا يوجد تقسيم تلقائي بعد.
- **لا تراجع/إعادة (Undo/Redo)** عبر تاريخ التحرير.
- **مفاتيح API تُخزَّن نصّاً صريحاً** في `localStorage`.
- **خارج النطاق حالياً:** التحرير متعدّد المسارات، الموسيقى والمؤثّرات، حرق الترجمة داخل الفيديو، قوالب Shorts العمودية (9:16)، والتعاون السحابي.

</div>

<div dir="rtl">

# وثيقة متطلبات المنتج (PRD) — محرّر الفيديو العربي

| | |
|---|---|
| **اسم المشروع** | Arabic Video Editor (aravid) |
| **الإصدار** | 0.1.0 |
| **نوع التطبيق** | تطبيق سطح مكتب (Tauri 2 + Next.js 16 + React 19) |
| **المنصّات** | Windows / macOS / Linux |
| **آخر تحديث للوثيقة** | 2026-07-04 |
| **الحالة** | قيد التطوير — المراحل 1–4 مُنفَّذة |

---

## 1. نظرة عامة (Overview)

**محرّر الفيديو العربي** تطبيق سطح مكتب يتيح تحرير مقاطع الفيديو **عبر تحرير النصّ** بدلاً من الخطّ الزمني التقليدي. يفرّغ التطبيق صوت الفيديو إلى نصّ مع طوابع زمنية دقيقة لكل كلمة، فيصبح حذف جزء من الفيديو مجرّد **حذف كلمات من النصّ** — تُترجَم آلياً إلى قصّ الأجزاء المقابلة من الفيديو.

الجمهور المستهدَف: **صنّاع المحتوى العربي** (يوتيوب، بودكاست، دورات تعليمية) الذين يحتاجون تحريراً سريعاً دون خبرة في برامج المونتاج المعقّدة.

### 1.1 المشكلة

- تحرير الفيديو التقليدي بطيء ويتطلّب مهارة في التعامل مع الخطّ الزمني.
- إزالة السكتات وكلمات الحشو (أه، يعني، آآ) يدوياً مُرهِق ويستغرق ساعات.
- أدوات التفريغ العربية ضعيفة، وأخطاء التفريغ (ASR) تحتاج مراجعة يدوية.
- استخراج مقاطع قصيرة (Shorts / Reels) من محتوى طويل يتطلّب مشاهدة كاملة.

### 1.2 الحلّ المقترح

محرّر يعتمد على النصّ يوفّر:
1. تفريغاً صوتياً عربياً عالي الدقة عبر عدّة مزوّدين.
2. حذفاً "ناعماً" للكلمات (Soft Delete) يُترجَم لقصّ زمني تلقائي.
3. كشف السكتات آلياً لإزالتها بضغطة.
4. مراجعة ذكاء اصطناعي لأخطاء التفريغ مع اقتراحات تصحيح.
5. توليد مقاطع قصيرة (Shorts) ذكية من أفضل اللحظات.
6. تصدير الفيديو النهائي عبر FFmpeg دون إعادة ترميز مكلفة.

---

## 2. الأهداف ومقاييس النجاح

### 2.1 الأهداف

| # | الهدف |
|---|---|
| 1 | تقليص زمن تحرير حلقة مدّتها ساعة من عدّة ساعات إلى أقل من 30 دقيقة |
| 2 | تفريغ عربي بدقّة كلمات ≥ 90% مع طوابع زمنية على مستوى الكلمة |
| 3 | سير عمل خطّي واضح (اختيار → تفريغ → تحرير → تصدير) يفهمه غير المختصّ |
| 4 | العمل بالكامل محلياً (معالجة الفيديو) مع استدعاءات سحابية للـ AI فقط |

### 2.2 مقاييس النجاح (KPIs)

- **زمن الوصول للتصدير الأول** < 15 دقيقة لمستخدم جديد.
- **نسبة الكلمات المُصحَّحة يدوياً** بعد التفريغ < 10%.
- **معدل نجاح التصدير** (بدون فشل FFmpeg) ≥ 98%.

---

## 3. البنية التقنية

```
┌─────────────────────────────────────────────┐
│  الواجهة الأمامية (Next.js 16 / React 19)     │
│  - page.tsx        سير العمل الخطّي          │
│  - EditorStudio    الاستوديو المركزي          │
│  - TextEditor      محرر النصّ التفاعلي         │
│  - VideoPlayer     مشغّل ذكي يتخطّى المحذوف     │
│  - ExportPanel     التصدير + Shorts           │
│  - SettingsModal   المفاتيح والنماذج          │
└───────────────────┬─────────────────────────┘
                    │ Tauri invoke (IPC)
┌───────────────────┴─────────────────────────┐
│  الخلفية (Rust / Tauri 2)                     │
│  - commands.rs   أوامر المعالجة               │
│  - FFmpeg        استخراج/كشف/قصّ الفيديو       │
│  - HTTP clients  مزوّدو التفريغ والـ LLM        │
└─────────────────────────────────────────────┘
```

- **التخزين:** الإعدادات والمفاتيح في `localStorage` (المفتاح `arabic-video-editor:settings-v2`).
- **معالجة الوسائط:** FFmpeg محلياً بالكامل (لا رفع للفيديو للسحابة).
- **الاستدعاءات السحابية:** فقط نصّ التفريغ يُرسَل لمزوّدي الـ AI (تفريغ + مراجعة + Shorts).

---

## 4. المتطلبات الوظيفية (Functional Requirements)

سير العمل مقسّم إلى مراحل، تمثّل الحالات `Stage = "pick" | "extracted" | "silence" | "transcribed"` ثم وضع الاستوديو.

### 4.1 المرحلة 1 — استيراد الفيديو واستخراج الصوت

| المعرّف | المتطلب |
|---|---|
| FR-1.1 | اختيار ملف فيديو عبر نافذة نظام (mp4, mkv, mov, avi, webm, ...). |
| FR-1.2 | عرض اسم الملف وحجمه بعد الاختيار. |
| FR-1.3 | فحص توفّر FFmpeg وعرض تحذير إن كان غائباً. |
| FR-1.4 | استخراج المسار الصوتي من الفيديو كملف منفصل للمعالجة. |

### 4.2 المرحلة 2 — كشف السكتات والتفريغ

| المعرّف | المتطلب |
|---|---|
| FR-2.1 | كشف فترات الصمت مع حدّ ضوضاء قابل للضبط (افتراضي −30dB) وحدّ أدنى للمدّة (افتراضي 0.5s). |
| FR-2.2 | عرض قائمة السكتات مع إمكانية معاينة صوتية لكل سكتة. |
| FR-2.3 | تحديد سكتات لإزالتها قبل الدخول للاستوديو. |
| FR-2.4 | تفريغ الصوت لنصّ عربي عبر أحد المزوّدين: **Groq** / **OpenAI Whisper** / **Speechmatics**. |
| FR-2.5 | إرجاع النصّ ككلمات منفصلة، كلٌّ بطابع زمني (start/end). |

### 4.3 المرحلة 3 — الاستوديو (محرّر النصّ)

| المعرّف | المتطلب |
|---|---|
| FR-3.1 | عرض كامل التفريغ ككلمات قابلة للنقر باتجاه RTL. |
| FR-3.2 | **تحديد** كلمة (نقر)، نطاق (Shift+نقر)، متعدّد (Ctrl+نقر)، الكل (Ctrl+A). |
| FR-3.3 | **حذف ناعم** للكلمات المحدّدة (Delete/Backspace) — تظهر بخطّ شطب رمادي دون فقدان البيانات. |
| FR-3.4 | **استعادة** كلمة محذوفة بالنقر المزدوج. |
| FR-3.5 | **تحرير إنلاين** لنصّ الكلمة (نقر مزدوج) مع الاحتفاظ بتوقيتها الزمني ووسمها كـ "مُعدّلة". |
| FR-3.6 | النقر على كلمة يقفز بالفيديو لوقتها (كمحرّرات التفريغ الاحترافية). |
| FR-3.7 | إبراز الكلمة المُشغَّلة حالياً أثناء تشغيل الفيديو. |
| FR-3.8 | تحويل الكلمات المحذوفة تلقائياً إلى **فترات زمنية مستبعَدة** (مع دمج المتجاورة). |
| FR-3.9 | عرض إحصائيات حيّة: عدد الكلمات النشطة/المحذوفة/المحدّدة والمدّة المستبعَدة. |

### 4.4 المرحلة 3+ — مراجعة الأخطاء بالذكاء الاصطناعي

| المعرّف | المتطلب |
|---|---|
| FR-4.1 | إرسال التفريغ لنموذج LLM لكشف المواضع غير المنطقية / أخطاء التفريغ. |
| FR-4.2 | تمييز الكلمات المشبوهة بخطّ متعرّج ملوّن حسب الخطورة (high=أحمر، medium=كهرماني، low=أزرق). |
| FR-4.3 | عرض سبب كل خطأ واقتراح تصحيح. |
| FR-4.4 | **قبول التصحيح** يطبّق الاقتراح على النطاق ويزيل العلامة. |
| FR-4.5 | إزالة علامة الخطأ تلقائياً عند تصحيح الكلمة يدوياً. |

### 4.5 المرحلة 4 — التصدير و Shorts

| المعرّف | المتطلب |
|---|---|
| FR-5.1 | تصدير الفيديو النهائي بعد إزالة كل الفترات المستبعَدة عبر FFmpeg. |
| FR-5.2 | عرض ملخّص التصدير: المسار، الحجم، المدّة الأصلية والنهائية. |
| FR-5.3 | توليد اقتراحات مقاطع قصيرة (3–5) عبر LLM من أفضل اللحظات (10–60 ثانية). |
| FR-5.4 | لكل Short: عنوان جذّاب + نطاق زمني + سبب الاختيار. |
| FR-5.5 | استخراج أي Short كملف فيديو منفصل. |

### 4.6 الإعدادات

| المعرّف | المتطلب |
|---|---|
| FR-6.1 | إدارة مفاتيح API لكل مزوّد (Groq, OpenAI, Speechmatics, Anthropic, Gemini). |
| FR-6.2 | **سجلّ نماذج نصية** مرن: اسم + مزوّد + معرّف موديل + مفتاح. |
| FR-6.3 | ربط كل **مهمة نصية** (كشف الأخطاء / توليد Shorts) بنموذج مختار. |
| FR-6.4 | **برومت قابل للتعديل** لكل مهمة مع إمكانية الاستعادة للافتراضي. |
| FR-6.5 | حفظ فوري في localStorage ومزامنة بين كل المكوّنات المفتوحة. |
| FR-6.6 | ترحيل الإعدادات القديمة (migration) تلقائياً عند تغيّر البنية. |

---

## 5. المتطلبات غير الوظيفية (Non-Functional)

| المجال | المتطلب |
|---|---|
| **الأداء** | معالجة الفيديو بلا إعادة ترميز حيثما أمكن (stream copy)؛ بحث ثنائي للفترات المستبعَدة. |
| **التوطين** | واجهة عربية بالكامل باتجاه RTL؛ دعم افتراضي للغة `ar` في التفريغ. |
| **الخصوصية** | لا يُرفع الفيديو لأي خادم؛ فقط نصّ التفريغ يُرسَل لمزوّدي الـ AI باختيار المستخدم. |
| **الموثوقية** | معالجة أخطاء موحّدة (`AppError`) من الخلفية مع رسائل عربية واضحة. |
| **قابلية العمل دون Tauri** | الكشف عن بيئة Tauri؛ رسالة واضحة أنّ التطبيق يعمل على سطح المكتب فقط. |
| **الأمان** | المفاتيح تُخزَّن محلياً؛ لا تُسجَّل في اللوقات. |

---

## 6. المزوّدون المدعومون

### 6.1 التفريغ الصوتي (Transcription)
- **Groq** — `whisper-large-v3`, `whisper-large-v3-turbo` (الافتراضي).
- **OpenAI** — `whisper-1`, `gpt-4o-transcribe`, `gpt-4o-mini-transcribe`.
- **Speechmatics** — `enhanced`, `standard` (واجهة غير متزامنة قائمة على المهام).

### 6.2 النماذج النصية (LLM — كشف الأخطاء + Shorts)
- **OpenAI** — `gpt-4o`, `gpt-4o-mini`, `gpt-4.1`.
- **Anthropic (Claude)** — `claude-sonnet-4`, `claude-opus-4`.
- **Gemini (Google)** — `gemini-2.5-flash` (افتراضي للكشف), `gemini-2.5-pro`, `gemini-2.0-flash`.

---

## 7. خارج النطاق (Out of Scope) — للإصدار الحالي

- تحرير متعدّد المسارات (Multi-track) أو إضافة موسيقى/مؤثرات.
- الترجمة (Subtitles/Captions) وتصديرها كملفات SRT.
- التعاون السحابي أو حفظ المشاريع على الخادم.
- تفريغ لغات غير العربية كتجربة أساسية (مدعوم تقنياً لكن غير مُحسَّن).
- التراجع/الإعادة (Undo/Redo) الكامل عبر تاريخ التحرير.

---

## 8. مخاطر وافتراضات

| المخاطرة / الافتراض | التخفيف |
|---|---|
| توفّر FFmpeg على جهاز المستخدم | فحص عند الإقلاع + تعليمات تثبيت. |
| دقّة التفريغ العربي متفاوتة بين المزوّدين | طبقة مراجعة AI + تحرير يدوي سهل. |
| تكلفة استدعاءات الـ AI على المستخدم | المستخدم يوفّر مفاتيحه الخاصّة ويتحكّم بالنموذج. |
| اختلافات إصدار Next.js 16 عن المعتاد | الالتزام بمراجعة أدلّة `node_modules/next/dist/docs/` قبل أي كود. |

---

## 9. خارطة طريق مستقبلية (مقترحة)

1. تصدير الترجمات (SRT/VTT) من التفريغ المُحرَّر.
2. إزالة كلمات الحشو تلقائياً (أه، يعني…) بضغطة واحدة.
3. Undo/Redo كامل.
4. حفظ/استئناف المشاريع محلياً.
5. قوالب Shorts عمودية (9:16) مع عنونة تلقائية.

</div>

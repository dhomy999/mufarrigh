<div dir="rtl">

# خطة تطوير aravid — فصل مسار التفريغ النصّي عن مسار تحرير الفيديو

| | |
|---|---|
| **المستند المرجعي** | `FEATURES.md` (وصف الإصدار الحالي) |
| **التاريخ** | 2026-07-26 |
| **الحالة** | مقترح للتنفيذ |
| **الجمهور المستهدف أولاً** | مفرّغو النصوص (محاضرات ودروس، متحدث واحد غالباً) |
| **مصادر الإدخال** | صوت وفيديو بنسبة متقاربة |

---

## 0. الملخّص التنفيذي

**المشكلة:** التطبيق يملك نموذج بيانات واحداً دلالته زمنية بحتة (`كلمات بطوابع ← فترات مستبعَدة ← قصّ FFmpeg`). لا يوجد فيه مفهوم «مستند»: لا فقرات، لا ترقيم، لا بنية. لذلك انجرّ حتماً نحو تحرير الفيديو، وأي محاولة لإضافة «تصدير Word» فوق هذا النموذج ستنتج ملفاً رديئاً.

**القرار:** **افصل السطح، ووحّد النواة.** تطبيق واحد، نموذج بيانات واحد مُرقّى إلى تدفّق توكِنات، وسطحان (`TextStudio` / `VideoStudio`) يقرآن نفس المخزن ويعرضانه ويصدّرانه بطريقتين مختلفتين. **لا نسخ كود، ولا تشعّب في البيانات.**

**نقطة الإطلاق المستهدفة:** نهاية المرحلة ٢ — حين يستطيع مفرّغ حقيقي أن يأخذ محاضرة مدّتها ساعتان ويخرج بملف Word مرقّم ومقسّم إلى فقرات معنونة. كل ما بعد ذلك تحسين لا وجود.

---

## 1. التشخيص

### 1.1 جمهوران، وتعريفان مختلفان للمنتَج

| | 📝 مفرّغ النصوص | 🎬 صانع الشورتس |
|---|---|---|
| **المنتَج النهائي** | مستند — والزمن أداة تنقّل | خطّ زمني — والنصّ لوحة تحكّم |
| **معنى «حذف كلمة»** | لا تظهر في المستند | يُقصّ الفيديو |
| **معنى «السكتة»** | تلميح لبداية فقرة | مادة تُحذف |
| **الإدخال المعتاد** | صوت أو فيديو | فيديو |
| **مقياس النجاح** | نصّ نظيف مقروء | مقطع جذّاب |

### 1.2 لماذا لا تكفي إضافة زرّ «تصدير Word»

لأن ما تحت الزرّ ليس نصّاً، بل قائمة كلمات عارية بلا ترقيم ولا فقرات ولا بنية. المشكلة في الطبقة السفلى لا العليا.

---

## 2. القرار المعماري

### 2.1 نواة واحدة، سطحان

```
                    ┌──────────────────────────┐
                    │   TranscriptDocument     │  ← النواة المشتركة
                    │   (تدفّق توكِنات موحّد)    │
                    └────────────┬─────────────┘
                                 │
                ┌────────────────┴────────────────┐
                │                                 │
        ┌───────▼────────┐               ┌────────▼───────┐
        │   TextStudio   │               │  VideoStudio   │
        │  فقرات متدفّقة  │               │ شرائح + خط زمني │
        └───────┬────────┘               └────────┬───────┘
                │                                 │
        DOCX / PDF / TXT / MD              MP4 / Shorts / SRT
```

### 2.2 نموذج التوكِن (أهم تغيير تقني في الخطة)

انتقال من `Word[]` إلى تدفّق توكِنات:

```ts
type Token = {
  id: string
  kind: 'word' | 'punct' | 'break'      // break = فاصل فقرة
  text: string
  start?: number                         // الترقيم والفواصل بلا زمن
  end?: number
  confidence?: number
  speaker?: string                       // S1, S2 … (اختياري، مؤجَّل)
  status: 'active' | 'removed'
  origin: 'asr' | 'user' | 'ai' | 'dict'
  originalText?: string                  // للتراجع
}

type TranscriptDocument = {
  tokens: Token[]
  paragraphs: { startTokenId: string; endTokenId: string; heading?: string }[]
  speakers?: Record<string, string>
  meta: { sourceKind: 'audio' | 'video'; duration: number; provider: string }
}
```

**لماذا يحلّ هذا كل شيء:**

| الفائدة | التفصيل |
|---|---|
| **الترقيم بلا تعارض** | علامة الترقيم توكِن **بلا مدّة** — يقرأها المستند ويتجاهلها مصدّر الفيديو تلقائياً |
| **`removed` صالحة في المسارين** | حذف حشو «يعني/آآ» في وضع النصّ يصير قصّاً مفيداً إن صُدِّر فيديو لاحقاً — ميزة لا مشكلة |
| **قابلية التوسّع** | المتحدثون والتشكيل والوسوم كلها تركب على نفس المصفوفة |
| **ترحيل غير مدمّر** | كل `Word` قديمة ← توكِن `kind:'word'` بلا فقدان |

### 2.3 قاعدة حديدية: الذكاء الاصطناعي لا يعيد كتابة المصفوفة أبداً

كل عملية ذكية (ترقيم، تصحيح، عناوين، تشكيل) تُرجِع **اقتراحات** تُحاذى على التوكِنات الموجودة بخوارزمية LCS/diff، وتُطبَّق بمراجعة صريحة.

> **شرط لا يُخترق:** أي مخرَج من نموذج يغيّر **عدد** الكلمات في نطاق ما لا يُطبَّق تلقائياً إطلاقاً.

طبقة «مراجعة التفريغ بالذكاء الاصطناعي» الحالية هي النواة الصحيحة لهذا — عمّمها إلى **طبقة اقتراحات واحدة** تخدم كل الميزات بدل تكرار المنطق.

**المبرّر:** ترقيم العربية الآلي لا يزال مجالاً بحثياً غير محلول، وأفضل النتائج المنشورة (AraPunc / XLM-RoBERTa) بعيدة عن الكمال. طبقة المراجعة البشرية **شرط تصميمي، لا رفاهية**.

### 2.4 خطة الترحيل

1. دالة `migrateProject(v1) → v2` تعمل عند الفتح، مع `schemaVersion` في ملف المشروع.
2. الاحتفاظ بنسخة `.bak` من الملف الأصلي قبل أول كتابة بالنموذج الجديد.
3. اختبار على مشاريع حقيقية قديمة **قبل** الإطلاق.

---

## 3. الفصل بين المسارين

### 3.1 ما يتغيّر في كل وضع

الوضع يُختار عند إنشاء المشروع ويُخزَّن كـ `project.mode`، ويبقى **قابلاً للتبديل في أي وقت**.

| | 📝 مسار النصّ | 🎬 مسار الفيديو |
|---|---|---|
| **الإدخال** | حقل موحّد يقبل الصوت والفيديو ويكتشف النوع | فيديو |
| **استخراج الصوت** | يُتخطّى كلياً إن كان المصدر صوتياً | خطوة قائمة |
| **كشف السكتات** | مخفي — يُشغَّل صامتاً كتلميح لتقسيم الفقرات | خطوة ظاهرة |
| **المحرّر** | فقرات متدفّقة مقروءة | شرائح كلمات + خطّ زمني |
| **المشغّل** | شريط نقل نحيف دائم + لوحة فيديو مصغّرة قابلة للطيّ | مشغّل رئيسي كبير |
| **المخرجات** | DOCX / PDF / TXT / MD (+ SRT اختياري) | MP4 / Shorts / SRT (+ نصّ اختياري) |

### 3.2 ثلاث قواعد تنفيذية غير قابلة للتفاوض

1. **أخفِ ولا تُعطِّل.** زرّ رمادي مع تلميح = ارتباك. المفرّغ يجب ألّا يرى كلمة «Shorts» ولو مرة واحدة.
2. **مبدّل صريح في ترويسة الاستوديو:** «حوِّل إلى مسار الفيديو» / «حوِّل إلى مسار النصّ». سيناريو «فرّغت المحاضرة ثم أردت شورتس منها» واقعي — وبما أن نصف المادة فيديو أصلاً، فهذا الجسر أساسي لا ثانوي. الوضع **عرض** لا **تشعّب بيانات**.
3. **الإعدادات تنقسم أيضاً.** «مهام النماذج النصية» حالياً مهمّتان مختلطتان (كشف الأخطاء / توليد Shorts) — حوّلها إلى **سجلّ مهام مجموعة حسب المسار**، فلا يرى مستخدم إعدادات لا تخصّه.

### 3.3 التقسيم المقترح للشاشات

```
src/app/
  page.tsx                    ← بطاقتان: «تفريغ نصّي» (افتراضي) / «تحرير فيديو»
  studio/
    text/                     ← TextStudio
    video/                    ← VideoStudio (الاستوديو الحالي، مُعاد تسميته)
src/core/
  document/                   ← نموذج التوكِن + العمليات (مشترك)
  suggestions/                ← طبقة الاقتراحات الموحّدة (مشترك)
  export/
    docx.ts  pdf.ts  txt.ts   ← مسار النصّ
    video.ts shorts.ts srt.ts ← مسار الفيديو
```

---

## 4. مكاسب مجانية من المزوّدين الحاليين

**ثلاثة أرباع مسار النصّ متاح عندك الآن ويُهدَر.**

| المصدر | المكسب | الحالة |
|---|---|---|
| **Speechmatics `json-v2`** | يُرجِع الترقيم كتوكِنات مستقلّة من نوع `punctuation` | **يُسقَط حالياً عند التحويل إلى `Word[]`** |
| **Speechmatics `additional_vocab`** | قاموس مخصّص للأسماء والمصطلحات يُمرَّر **قبل** التفريغ، مع نطق بديل اختياري | غير مستخدم — وهو الحلّ الصحيح لـ«القاموس» |
| **Speechmatics `punctuation_overrides`** | حساسية الترقيم بقيمة بين ٠ و١ | غير مستخدم |
| **Speechmatics — وسم الحشو** | تعليم كلمات التردّد («أم»، «آه») وهو بالضبط ما يحذفه المفرّغ | غير مستخدم |
| **Whisper (OpenAI/Groq)** | حقل `text` **مرقّم** بجانب `words[]` **غير المرقّمة** ← محاذاة بسيطة تستعيد الترقيم | غير مستغَل |

**النتيجة:** استدعاء الـ LLM للترقيم ينزل من «الآلية الأساسية» إلى **طبقة تحسين اختيارية**. فارق ضخم في التكلفة والزمن والموثوقية على محاضرة ساعتين.

**ملاحظة عن فصل المتحدثين:** Speechmatics يضيف خاصية `speaker` لكل كائن كلمة وترقيم عند تفعيله — لكن بما أن المحتوى أحادي المتحدث غالباً، فهذه الميزة **مؤجَّلة إلى المرحلة ٥**.

---

## 5. محرّك تقسيم الفقرات — المشكلة التقنية الأولى

مع متحدث واحد لساعتين، لا توجد إشارة «تغيّر المتكلّم». النتيجة الافتراضية جدار نصّ من ١٥ ألف كلمة، وهو وحده يكفي لإفراغ مخرَج Word من قيمته.

### 5.1 الخوارزمية

```
1. اجمع كل الفجوات الزمنية التي تقع بعد توكِن ترقيم ختامي (. ؟ !)
2. threshold = المئين 75 لهذا التوزيع        ← تكيّفية: المتحدث يُقارَن بنفسه
3. لكل نهاية جملة:
     if (gap >= threshold)  → أدرِج break
4. سقف: عند تجاوز 120 كلمة → أجبر break عند أقرب نهاية جملة
5. أرضية: أي فقرة < 25 كلمة → ادمجها مع التالية
```

**لماذا العتبة تكيّفية:** المحاضر البطيء والسريع لا يتشابهان؛ `if (gap > 1.5s)` ثابتة تفشل على أحدهما حتماً.

**لماذا الشرط مركّب:** الفجوة وحدها تكسر الجُمل في منتصفها؛ الترقيم وحده ينتج فقرة لكل جملة. الشرطان معاً.

### 5.2 تمريرة العناوين — آمنة تماماً

بعد التقسيم، استدعاء واحد لكل ~٣٠ دقيقة يُرجِع فقط:

```json
[{ "beforeTokenId": "t_4821", "heading": "شروط صحة الحديث" }]
```

**لا يلمس كلمة واحدة** ⇒ خطر الانزياح صفر، ولا حاجة لطبقة محاذاة. والمخرَج فهرس محاضرة جاهز في ملف Word.

> **بديل يستحق اختباراً واحداً:** ميزة Chapters في Speechmatics تُرجِع عناوين وملخّصات بأوقات، ومدعومة للعربية — لكن التوثيق ينصّ على أنها تعمل بأفضل صورة مع تفعيل فصل المتحدثين، وهو غير مجدٍ في محتوى أحادي المتحدث. جرّبها على ملف واحد؛ وإن خذلتك فتمريرة العناوين أعلاه تغنيك بتكلفة أقل.

---

## 6. خارطة الطريق

### المرحلة ٠ — أساسات إلزامية *قبل* أي فصل

أربعة من بنود «قيود الإصدار الحالي» ليست نواقص تجميلية — هي **حواجز تمنع مسار النصّ من الوجود**.

| # | المهمة | لماذا هي حاجب |
|---|---|---|
| ٠.١ | نموذج التوكِن + الترحيل | كل ما بعده مبني عليه |
| ٠.٢ | **كسر سقف 25MB** بالتقسيم عند حدود الصمت | المفرّغ يعمل على ملفات ١–٣ ساعات. **بلا هذا لا يوجد مسار نصّ** |
| ٠.٣ | شريط تقدّم + إلغاء | ملف ساعتين بلا مؤشّر = تطبيق «معلّق» |
| ٠.٤ | Undo/Redo على مستوى العمليات | محرّر مستندات بلا تراجع غير قابل للاستخدام |
| ٠.٥ | اختيار مجلّد المخرجات | لا أحد يقبل ملف Word في مجلّد كاش |

> **توفير مهم في ٠.٢:** لا تبنِ مقسِّماً جديداً — **كاشف السكتات الذي بنيته هو بالضبط الأداة المطلوبة**. قسّم عند أطول فترة صمت داخل نافذة ~٢٠ دقيقة، أضف إزاحة زمنية لكل قطعة، ثم ادمج. إعادة استخدام لا إعادة بناء.

**معيار القبول:** تفريغ ملف مدّته ساعتان من البداية للنهاية بمؤشّر تقدّم، مع إمكانية الإلغاء، وحفظ المخرَج في مجلّد يختاره المستخدم.

---

### المرحلة ١ — الفصل الهيكلي (بلا ميزة جديدة واحدة)

`project.mode` ← شاشة اختيار المسار (**النصّ هو الافتراضي**) ← فصل `TextStudio` / `VideoStudio` فوق نفس المخزن ← توجيه الخطوات ← لوحة تصدير لكل مسار ← مبدّل المسار ← تقسيم الإعدادات.

**معيار القبول:** لا يرى أي مستخدم عنصر واجهة لا يخصّ مساره. **قدرات جديدة: صفر. ارتباك: صفر.**

> أنجِز هذه المرحلة كاملة قبل أي شيء آخر — هي الإجابة الحقيقية على سؤال الفصل.

---

### المرحلة ٢ — جوهر مسار النصّ *(نقطة الإطلاق)*

| # | المهمة |
|---|---|
| ٢.١ | استيراد موحّد يقبل الصوت والفيديو ويكتشف النوع؛ تخطّي الاستخراج للصوت |
| ٢.٢ | التقاط الترقيم المجاني (توكِنات Speechmatics + محاذاة `text`/`words[]` في Whisper) |
| ٢.٣ | **محرّك تقسيم الفقرات** (القسم ٥) |
| ٢.٤ | عرض الفقرات المتدفّق + المشغّل المتكيّف |
| ٢.٥ | سرعة تشغيل ٠٫٥×–٢× + **رجوع تلقائي ثانيتين عند الإيقاف** |
| ٢.٦ | تصدير DOCX صحيح (الملحق أ) |

**عن ٢.٤:** الكلمات تبقى `spans` تحمل الزمن، لكنها **تُقرأ كمستند**. احتفظ بالنقر-للانتقال وإبراز الكلمة الجارية — هذا سحر التطبيق. أضف: الكتابة الحرّة (نصّ جديد يرث زمناً مُستنتَجاً من جيرانه)، تقسيم ودمج الفقرات، بحث واستبدال.

**عن ٢.٥:** أعلى عائد مقابل أقل جهد في الخطة كلها. الميزتان تضاعفان سرعة المفرّغ حرفياً، وكلفتهما ساعات لا أيام. **لا تؤجّلهما.**

**معيار القبول:** محاضرة ساعتان ← ملف Word مرقّم، مقسّم إلى فقرات، معنون، باتجاه RTL صحيح.

---

### المرحلة ٣ — طبقة القاموس والتصحيح

| # | المهمة |
|---|---|
| ٣.١ | قاموس المستخدم (عام + خاص بالمشروع)، استبدال نصّي/regex، **ويُحقَن في `additional_vocab` قبل التفريغ** لا بعده فقط |
| ٣.٢ | «تعلَّم من تصحيحاتي»: عند تعديل كلمة يدوياً، اقترح إضافتها للقاموس |
| ٣.٣ | تمريرة العناوين بالنموذج (القسم ٥.٢) |
| ٣.٤ | **كشف الآيات والأحاديث وتنسيقها** ← أدناه |
| ٣.٥ | hunspell-ar لتعليم الكلمات المجهولة فقط + توحيد طبقة الاقتراحات |

#### ٣.٤ — الميزة التي لا يستطيع أحد تقليدها

المحاضر في المحتوى الشرعي يستشهد بآية أو حديث كل بضع دقائق، **ونماذج التفريغ تُحرّف الآيات باستمرار** لأنها مدرَّبة على كلام عادي.

الكشف التلقائي + المطابقة مع مصدر موثوق + التنسيق بـ ﴿ ﴾ مع اسم السورة ورقمها ليس تجميلاً — هو **تصحيح لأخطاء تفريغ حقيقية**. ولا يستطيع أي منافس عالمي مقاربته، والبنية التحتية له موجودة أصلاً في تطبيق الحديث.

**التوصية:** قدّمه على ٣.٥، واعتبره واجهة تسويقية للمنتج كلّه.

#### تحذير بشأن ٣.٥

قاموس AyaSpell العربي لـ Hunspell متاح برخصة ثلاثية GPL/LGPL/MPL بدعم من مدينة الملك عبدالعزيز للعلوم والتقنية — لكن بشرطين:

- آخر إصدار جدّي يعود لعام ٢٠١٤، وصرف العربية الغني يجعله **كثير الإنذارات الكاذبة** ⇒ استخدمه **لتعليم الكلمات المجهولة فقط**، لا كمرجع تصحيح.
- رخصة GPL قد تقيّدك إن كانت هناك نيّة تسويق تجاري.

---

### المرحلة ٤ — تعزيز مسار الشورتس

| # | المهمة | الحالة الآن |
|---|---|---|
| ٤.١ | **SRT مقصوص على نطاق كل مقطع** عند استخراجه | التصدير حالياً على مستوى الفيديو كامل فقط |
| ٤.٢ | قوالب عمودية 9:16 | مذكورة «خارج النطاق» — وهي المطلب الأول لصانع الشورتس |
| ٤.٣ | ترجمة محروقة داخل الفيديو | مذكورة «خارج النطاق» |
| ٤.٤ | معاينة المقطع قبل الاستخراج | غير موجودة |

---

### المرحلة ٥ — إضافات وتلميع

- فصل المتحدثين + لوحة تسمية (للمشاريع متعدّدة المتحدثين)
- التشكيل الآلي (اختياري)
- تشفير مفاتيح الـ API خارج `localStorage`
- تصدير/استيراد المشروع
- تضمين FFmpeg مع التطبيق

---

## 7. المخاطر والتخفيف

| المخاطرة | الأثر | التخفيف |
|---|---|---|
| **فشل ترحيل نموذج التوكِن** | فقدان مشاريع مفرَّغة = فقدان مستخدم | نسخة `.bak` + اختبار على مشاريع حقيقية قبل الإطلاق |
| **انزياح المحاذاة عند تطبيق اقتراحات النموذج** | فقدان الطوابع الزمنية — وهي قيمة التطبيق كلها | قاعدة القسم ٢.٣: تغيّر عدد الكلمات ⇒ مراجعة إجبارية |
| **تكلفة الملفات الطويلة** | فاتورة مفاجئة على محاضرة ساعتين | **عرض تقدير للتكلفة قبل التشغيل** |
| **جدار نصّ في مخرَج Word** | المنتَج بلا قيمة رغم صحّته تقنياً | محرّك تقسيم الفقرات (القسم ٥) قبل أي تصدير |
| **رخصة GPL لـ hunspell-ar** | قيد قانوني عند التسويق | اجعله وحدة اختيارية منفصلة، أو استبدله بقائمة كلمات مرخّصة بحرّية |

---

## 8. خارج النطاق حالياً

التحرير متعدّد المسارات، الموسيقى والمؤثّرات، التعاون السحابي، الترجمة إلى لغات أخرى، النسخة السحابية أو الجوّالة.

---

## ملحق أ — مواصفة تصدير DOCX

**تحذير تقني:** لا يكفي ضبط المحاذاة يميناً. لملف عربي صحيح يجب:

- `<w:bidi/>` على خصائص كل **فقرة**
- `<w:rtl/>` على خصائص كل **run**

بدونهما يخرج الملف مشوّهاً عند خلط الأرقام أو النصّ اللاتيني.

**التوصية التنفيذية:** توليد الملف في طبقة JS عبر حزمة `docx` (دعم `bidirectional` / `rightToLeft` فيها أنضج من نظيره في مكتبات Rust)، ثم الحفظ عبر Tauri إلى المجلّد الذي يختاره المستخدم.

**خيارات التصدير المطلوبة:**

| الخيار | الافتراضي |
|---|---|
| إدراج عناوين الأقسام | مفعّل |
| طابع زمني قبل كل فقرة | معطّل |
| صفحة عنوان (اسم الملف، المدّة، التاريخ) | مفعّل |
| فهرس محتويات تلقائي | مفعّل عند وجود عناوين |
| الخطّ | خطّ عربي واضح للطباعة، حجم ١٤ |
| تضمين أسماء المتحدثين | معطّل (يظهر فقط عند تفعيل فصل المتحدثين) |

---

## ملحق ب — ترتيب التنفيذ المختصر

```
٠.١ نموذج التوكِن
٠.٢ كسر سقف 25MB          ← عبر كاشف السكتات الموجود
٠.٣ تقدّم + إلغاء
٠.٤ Undo/Redo
٠.٥ مجلّد المخرجات
───────────────────────────
١   الفصل الهيكلي كاملاً
───────────────────────────
٢.١ استيراد موحّد
٢.٢ التقاط الترقيم المجاني
٢.٣ محرّك تقسيم الفقرات    ← أخطر بند تقني
٢.٤ عرض الفقرات المتدفّق
٢.٥ سرعة + رجوع تلقائي     ← أعلى عائد لأقل جهد
٢.٦ تصدير DOCX
═══════════ إطلاق ═══════════
٣.١ القاموس + additional_vocab
٣.٢ تعلَّم من تصحيحاتي
٣.٣ تمريرة العناوين
٣.٤ كشف الآيات والأحاديث   ← الميزة التنافسية
٣.٥ hunspell-ar + توحيد الاقتراحات
───────────────────────────
٤   مسار الشورتس
٥   إضافات وتلميع
```

</div>
